# Argument parser for golang

Argparse module for golang. Lets you specify patterns that the command line must match. If it does not it will return a ParseError. If everything was according to your specified pattern, 
an instance of `*argparse.ParsedCommand` will be returned, for  you to deal with. 

Example use in [example](./example/main.go)
